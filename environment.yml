name: lidarforfuel
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy>=1.20.0
  - pandas>=1.3.0
  - scipy>=1.7.0
  - matplotlib>=3.3.0
  - rasterio>=1.2.0
  - pip
  - pip:
    - laspy>=2.0.0
    - lazrs>=0.6.0  # LAZ decompression backend (required for reading .laz files)

